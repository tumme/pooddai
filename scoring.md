# การให้คะแนนการพูด

เอกสารนี้อธิบายว่าคะแนนแต่ละตัวในแอปคำนวณมาจากอะไร โมเดล AI แต่ละตัวรับผิดชอบส่วนไหน
และมีข้อจำกัดอะไรที่ต้องรู้ก่อนแก้สูตร

- [1. คะแนนแต่ละส่วน](#1-คะแนนแต่ละส่วน)
- [2. โมเดล AI ที่ใช้ — ข้อดี/ข้อเสีย](#2-โมเดล-ai-ที่ใช้--ข้อดีข้อเสีย)
- [3. Flow การคิดคะแนน](#3-flow-การคิดคะแนน)
- [4. ข้อสังเกตเชิงออกแบบ](#4-ข้อสังเกตเชิงออกแบบ)

---

## 1. คะแนนแต่ละส่วน

คะแนนแบ่งเป็น **สองชั้นที่แยกกันชัดเจน** — ชั้นที่เข้าคะแนนรวม (คิดจากข้อความที่ถอดได้)
กับชั้นที่วัดจากเสียงจริงและแสดงแยกต่างหาก

### 1.1 ชั้นที่เข้าคะแนนรวม

ทั้งหมดอยู่ใน `lib/scoring/scoring_engine.dart`

#### Accuracy — ความถูกต้อง (น้ำหนัก 50%)

เทียบ token ทีละตัวด้วย Levenshtein alignment (DP + backtrace) แล้วให้แต้มรายคำ

| ผลการเทียบ | เงื่อนไข | แต้ม |
|---|---|---|
| `match` | ตรงเป๊ะ | 1.0 |
| `close` | `charSimilarity ≥ 0.6` — สะกดคล้าย มักคือออกเสียงเพี้ยน | 0.6 |
| `substitution` | ระบบได้ยินเป็นคนละคำ | 0 |
| `missing` | ไม่ได้พูดคำนี้ | 0 |
| `extra` | พูดเกินมา | ไม่นับ และ**ไม่หัก** |

```
accuracy = (แต้มรวม / จำนวน token เป้าหมาย) × 100
```

การแตก token ต่างกันตามภาษา — อังกฤษ/ฮีบรูตัดตามช่องว่าง ส่วนจีนตัดทีละตัวอักษร
(`ScoringEngine.tokenize`) ก่อนหน้านั้นทุกข้อความผ่าน `normalize` ที่ตัดนิกกุดของฮีบรู
ตัดเครื่องหมายวรรคตอน แปลงเป็นตัวพิมพ์เล็ก และยุบช่องว่าง

#### Completeness — ความครบถ้วน (น้ำหนัก 20%)

```
completeness = (จำนวนตำแหน่งที่พยายามพูด / จำนวน token เป้าหมาย) × 100
```

นับ `match + close + substitution` ทั้งหมด — วัดว่า *พยายามครบไหม* ไม่สนว่าถูกไหม
มีไว้เพื่อแยกกรณี "พูดผิด" ออกจาก "ข้ามไปเลย" ซึ่งต้องการคำแนะนำคนละแบบ

#### Fluency — ความคล่อง (น้ำหนัก 30%)

คิดจากความเร็วพูดเทียบช่วงธรรมชาติ เป็นกราฟรูปสี่เหลี่ยมคางหมู

```
100 ┤        ┌────────────┐
    │       ╱              ╲
 30 ┼──────┘                └──────
      60    90         175   240      (คำ/นาที)
    floor  low        high  ceiling
```

| ภาษา | หน่วย | floor | ช่วงธรรมชาติ | ceiling |
|---|---|---|---|---|
| อังกฤษ / ฮีบรู | คำ/นาที | 60 | 90–175 | 240 |
| จีน | ตัวอักษร/นาที | 80 | 140–320 | 450 |

อยู่ในช่วงธรรมชาติ = 100 คะแนน, หลุด floor หรือ ceiling = 30 คะแนน, ระหว่างนั้นไล่เป็นเส้นตรง
ภาษาจีนใช้ช่วงที่กว้างและสูงกว่าเพราะตัวอักษรจีนถี่กว่าคำ

ตัวหารคือ `speechDuration` ที่มาจาก **พลังงานเสียงจริง** (ตัดความเงียบหัวท้ายแล้ว)
ไม่ใช่ความยาวไฟล์ — ดู [Flow](#3-flow-การคิดคะแนน)

#### สูตรรวม

```dart
// เป้าหมายตั้งแต่ 3 หน่วยขึ้นไป (ประโยคปกติ)
overall = accuracy × 0.5 + completeness × 0.2 + fluency × 0.3

// เป้าหมายสั้นกว่า 3 หน่วย — คำศัพท์ใบเดียว, "謝謝", คำตอบสั้นในบทสนทนา
overall = accuracy × 0.7 + completeness × 0.3
```

เป้าหมายสั้นวัดความเร็วพูดไม่ได้: ความยาวเสียงถูกครอบด้วยเวลากดปุ่มกับหางเสียง
มากกว่าจังหวะการพูดจริง คำเดียวที่ออกเสียงตรงเป๊ะจึงเคยโดนหักเหลือ ~79 เพียงเพราะ
พูดช้าไปครึ่งวินาที `ScoreResult.fluencyCounts` บอกว่ารอบนั้นคิดความคล่องด้วยหรือไม่
และหน้าจอซ่อนแถบความคล่องเมื่อเป็น `false`

เกณฑ์สีที่ใช้ทั้งใน `result_panel.dart` และ `progress_screen.dart`

| ช่วง | สี |
|---|---|
| ≥ 85 | เขียว (`PoodColors.good`) |
| ≥ 65 | เหลือง (`PoodColors.fair`) |
| < 65 | แดง (`PoodColors.poor`) |

### 1.2 ชั้นที่วัดจากเสียงจริง

ทั้งสามตัวนี้แสดงแยกและเก็บลงประวัติ แต่ **ไม่ถูกถ่วงน้ำหนักเข้า `overall`** (ดู [ข้อสังเกต](#4-ข้อสังเกตเชิงออกแบบ))

#### Tone Score — วรรณยุกต์จีน (0–100)

`lib/scoring/tone_analyzer.dart`

จำเป็นเพราะ Whisper ถอดตัวอักษรจีนถูกได้ทั้งที่ผู้เรียนออกเสียงผิดวรรณยุกต์
ถ้าไม่มีตัวนี้ ผู้เรียนจะได้คะแนนเต็มทั้งที่เจ้าของภาษาฟังไม่รู้เรื่อง

1. **แบ่งพยางค์** — หาช่วงที่มีเสียงก้องต่อเนื่อง แล้ว merge คู่ที่ใกล้ที่สุด / split ช่วงที่ยาวที่สุด
   ตรงจุดพลังงานต่ำสุด จนได้จำนวนเท่ากับจำนวนตัวอักษรจีน (จำนวนพยางค์รู้แน่นอนอยู่แล้ว จึงใช้เป็นหลักยึดได้)
2. **แปลงเป็นสเกล Chao 1–5** ที่อ้างอิงช่วงเสียงของผู้พูดคนนั้นเอง (percentile 10/90 กันค่าหลุด)
   เสียงทุ้มกับเสียงแหลมจึงเทียบรูปทรงกันได้
3. **สุ่มรูปทรง 3 จุด** หลังตัดหัวท้ายทิ้งข้างละ 10% (ช่วงเปลี่ยนผ่านจากพยัญชนะต้นรบกวนรูปทรง
   แต่ตัดมากไปจะกลืนปลายที่ยกขึ้นของเสียง 3 หายไป)
4. **คำนวณระยะห่างจากรูปทรงเป้าหมาย**

```
distance = levelError × 0.4 + shapeError × 0.6
score    = clamp(1 − distance / 2.5, 0, 1) × 100
```

ถ่วง **ทิศทางการเปลี่ยน (shape) มากกว่าระดับเสียงสัมบูรณ์ (level)** เพราะขึ้น-ตกคือสิ่งที่ทำให้ความหมายเปลี่ยน

**เสียงเบา (neutral tone) ได้ 100 อัตโนมัติ** เพราะไม่มีรูปทรงตายตัว และคะแนนรวมของทั้งประโยค
เฉลี่ยเฉพาะพยางค์ที่ตัดสินได้ (`isJudged` — มีรูปทรงตายตัว *และ* จับเสียงก้องติด)

#### Melody Match — ทำนองเสียงทั้งประโยค (0–100)

`lib/audio/pitch_outline.dart` — เทียบเส้นเสียงของผู้เรียนกับเสียงต้นแบบที่สังเคราะห์จาก TTS

- ย่อทั้งสองคลิปเหลือ **48 จุด** บนแกนเวลา 0–1 (ผู้เรียนพูดช้ากว่าต้นแบบเกือบทุกครั้ง
  การเทียบทีละเฟรมตามเวลาจริงจะเลื่อนกันจนไม่มีความหมาย)
- แต่ละจุดเก็บเป็น**กึ่งเสียง**เทียบระดับเสียงกลางของคลิปนั้น และใช้**มัธยฐาน**ไม่ใช่ค่าเฉลี่ย
  เพราะ octave error เฟรมเดียวก็ดึงค่าเฉลี่ยลงได้ทั้งช่วง
- **หักค่าเฉลี่ยของแต่ละเส้นทิ้งก่อนเทียบ** — เป้าหมายคือ "ขึ้นลงตามจังหวะเดียวกัน"
  ไม่ใช่ "เสียงทุ้มแหลมเท่าเสียงสังเคราะห์" ซึ่งเป็นไปไม่ได้และไม่ช่วยให้ฟังรู้เรื่องขึ้น

```
score = clamp(1 − (error − 1) / 5, 0, 1) × 100
```

ต่างกันเฉลี่ยไม่เกิน 1 กึ่งเสียง = เต็ม, ตั้งแต่ 6 กึ่งเสียงขึ้นไป = 0
ต้องมีจุดที่ทั้งสองคลิปมีเสียงก้องพร้อมกัน **≥ 8 จุด** ไม่งั้นถือว่าเชื่อถือไม่ได้ (`isReliable`)

#### Continuity — ความต่อเนื่อง (0–100)

`lib/scoring/prosody_analyzer.dart`

| ค่าคงที่ | ค่า | ความหมาย |
|---|---|---|
| `_activeRatio` | 0.12 | ดัง ≥ 12% ของจุดที่ดังที่สุด = กำลังพูด |
| `_minPauseMs` | 120 | สั้นกว่านี้คือช่องว่างระหว่างพยางค์ปกติ |
| `hesitationMs` | 250 | ยาวกว่านี้คือ "ติด" ไม่ใช่จังหวะวรรค |

```
continuity = (ช่วงตั้งแต่เริ่มถึงจบพูด − เวลาที่หยุดคิดรวม) / ช่วงตั้งแต่เริ่มถึงจบพูด × 100
```

ไม่พึ่ง timestamp ของ Whisper เพราะประโยคสั้นมักถูกถอดออกมาเป็น segment เดียวทั้งประโยค
ซึ่งบอกไม่ได้ว่าผู้เรียนติดตรงไหน — `ProsodyAnalyzer.hintFor()` จึงระบุ**วินาทีที่ติด**ได้

### 1.3 ชั้นสะสม — Weakness Report

`lib/models/attempt.dart` — สรุปจุดอ่อนข้ามการฝึกหลายครั้ง เพื่อป้อนให้ AI แต่งแบบฝึกเจาะจง

- เรียงตาม **อัตราพลาด (miss rate)** ไม่ใช่จำนวนครั้ง เพื่อไม่ให้คำที่ฝึกบ่อยดูเหมือนเป็นปัญหา
  แค่เพราะเจอบ่อยกว่าคำอื่น
- ฐานการนับต้องผ่าน `ScoringEngine.normalize` + `tokenize` ตัวจริง ไม่ใช่ตัดตามช่องว่างเอง
  เพราะคำที่บันทึกว่าพลาดมาจาก diff ที่ normalize แล้ว ถ้าใช้คนละกติกา คำที่มีวรรคตอนติดจะหาฐานไม่เจอ
  แล้วอัตราพลาดกลายเป็น 1.0 ทุกคำ
- ฐานของวรรณยุกต์นับเฉพาะครั้งที่วิเคราะห์เสียงได้จริง (`toneScore != null`)

---

## 2. โมเดล AI ที่ใช้ — ข้อดี/ข้อเสีย

ทุกตัวรันบนเครื่อง 100% ตามข้อกำหนดของโปรเจกต์

### 2.1 Whisper GGML — ถอดเสียงเพื่อให้คะแนน

`lib/services/stt_service.dart` · ค่าเริ่มต้น `small` · เลือกได้ 3 รุ่นในหน้าตั้งค่า

| รุ่น | ขนาดโดยประมาณ | จุดขาย |
|---|---|---|
| `base` | ~148 MB | เร็วที่สุด ประหยัดพื้นที่ แต่ถอดพลาดบ่อยกว่า |
| `small` | ~488 MB | สมดุลระหว่างความแม่นกับความเร็ว (ค่าเริ่มต้น) |
| `medium` | ~1.5 GB | แม่นที่สุด แต่ช้าและกินแรมมาก |

**ข้อดี**

- ทำงานออฟไลน์ เสียงไม่ออกนอกเครื่อง ใช้ฟรีไม่จำกัดจำนวนครั้ง
- รองรับหลายภาษาในโมเดลเดียว — เปิดให้เลือกเฉพาะรุ่น multilingual โดยตั้งใจ เพราะรุ่น `.en`
  เล็กกว่าก็จริงแต่จะถอดฮีบรู/จีนออกมาเป็นขยะเงียบๆ โดยไม่มีอะไรบอกสาเหตุ
- ให้ segment timestamps ใช้เป็นตัวสำรองตอนคำนวณระยะเวลาพูดได้

**ข้อเสีย**

- **มองไม่เห็นวรรณยุกต์เลย** — ข้อจำกัดใหญ่ที่สุด เป็นเหตุผลที่ต้องมี `ToneAnalyzer`
- **มี "auto-correct" ในตัว** — โมเดลถูกเทรนให้ถอดออกมาเป็นประโยคที่ถูกไวยากรณ์
  จึงมักแก้คำที่ผู้เรียนออกเสียงเพี้ยนให้กลายเป็นคำที่ถูก ทำให้ accuracy สูงเกินจริง
- ถอดจีนออกมาเป็นตัวย่อ ต้องใช้ initial prompt `以下是繁體中文的句子。` โน้มให้เป็นตัวเต็ม
- ประโยคสั้นมักได้ segment เดียวทั้งประโยค ใช้จับจังหวะไม่ได้ (จึงต้องใช้ prosody แทน)
- โหลดครั้งแรกกินเน็ตเยอะ และ `medium` เสี่ยงโดนระบบฆ่ากลางทางบนเครื่องแรมน้อย
  (จึงเขียน `_fetch` เองให้สตรีมลงดิสก์ แทน `WhisperController.downloadModel` ที่อ่านทั้งไฟล์เข้าหน่วยความจำ)

### 2.2 Apple Speech Framework — ถอดสดระหว่างพูด

`lib/services/live_speech_service.dart` · `lib/scoring/live_match.dart`

**ข้อดี** — เห็นคำไล่ทีละคำระหว่างพูด ไฮไลต์ตามได้ทันที ไม่ต้องโหลดโมเดลเพิ่ม

**ข้อเสีย** — ผลหยาบกว่ามากเพราะคำยังไม่ถูกแก้ตามบริบท จึง**ไม่ใช้ให้คะแนน** ใช้แค่ไฮไลต์
เกณฑ์จับคู่จึงหลวมกว่า (`_threshold = 0.7`) และเดินหน้าอย่างเดียวไม่ถอยหลัง โดยยอมข้ามคำเป้าหมาย
ที่กลืนหายได้ไม่เกิน 1 คำ — ไฮไลต์ผิดจังหวะเล็กน้อยเสียหายน้อยกว่าไฮไลต์ไม่ขยับเลย

บางภาษา/บางเครื่องไม่รองรับ on-device ซึ่งกรณีนั้นแอปเลือก**ยอมไม่มีถอดสด** ดีกว่าส่งเสียง
ขึ้นเซิร์ฟเวอร์ Apple (`canTranscribe` บังคับ `onDevice` เสมอ)

### 2.3 Apple Foundation Models — โค้ชและสร้างแบบฝึก

`lib/services/llm/apple_fm_engine.dart` · `lib/services/coach_service.dart` · iOS 26+

**ข้อดี**

- โมเดลอยู่ใน OS แอปจึงไม่ต้องแบกน้ำหนักโมเดลเอง และ **ไม่แย่ง RAM กับ Whisper ที่รันอยู่แล้ว**
  — เหตุผลหลักที่เลือกทางนี้แทนการฝังโมเดลมาเอง
- บังคับรูปแบบคำตอบด้วย **JSON schema** ตั้งแต่ตอน decode ไม่ต้องแกะย่อหน้าที่โมเดลอธิบายฟุ้ง
- ฟรี ไม่จำกัดจำนวนครั้ง สร้างแบบฝึกใหม่ได้เรื่อยๆ โดยไม่ต้องต่อเน็ต

**ข้อเสีย**

- **ต้อง iOS 26+ และเปิด Apple Intelligence** — เครื่องส่วนใหญ่ยังใช้ไม่ได้ ทุกฟีเจอร์จึงต้องมีทางถอย
  (`CoachService._ruleBasedFeedback` สร้างคำแนะนำจาก diff ตรงๆ)
- โมเดลเล็ก คุณภาพจำกัด — ชอบแถมเกินหรือให้ขาด (ต้อง `minItems`/`maxItems` บังคับ),
  ตอบเป็นภาษาผิด (ต้องกรองด้วย `language.matchesScript`), ปฏิเสธเป็นครั้งคราวเพราะ guardrail
  (จึงลองซ้ำ 2 รอบเฉพาะ error ที่ retry แล้วมีโอกาสผ่าน)
- **schema บังคับได้แค่ "รูปร่าง" ไม่ใช่ "เนื้อหา"** — ยังต้องกรองด้วย `DrillPhrase.isUsable` อยู่ดี
- ไว้ใจคำอ่านจากโมเดลไม่ได้ — คำอ่านไทยของภาษาจีนจึงถอดจากคลังใน `lib/scoring/thai_reading.dart`
  ไม่ปล่อยให้โมเดลเดาเอง

### 2.4 YIN Pitch Tracker — ไม่ใช่ AI แต่เป็นแกนของ tone / melody / prosody

`lib/audio/pitch_tracker.dart` — DSP ล้วน 4 ขั้น: difference → cumulative mean normalize →
absolute threshold → parabolic interpolation

**ข้อดี** — ไม่ต้องเทรน ไม่มีน้ำหนักโมเดล และผลอธิบายได้ 100% ซึ่งสำคัญมากเวลาต้องบอกผู้เรียน
ว่าผิดตรงไหน นอกจากนี้ยังกัน octave error ได้ดีกว่า autocorrelation ธรรมดา ด้วยการเลือก tau
**ตัวแรกที่ผ่านเกณฑ์** แทนตัวที่ต่ำที่สุด

**ข้อเสีย** — O(window × tau) ต่อเฟรม หนักพอทำ UI กระตุก (ต้องโยนเข้า isolate),
พังกับเสียงรบกวนและเสียงกระซิบ, และ**การแบ่งพยางค์เป็นจุดอ่อนที่สุด** — ถ้าพูดติดกันจน
merge/split ไม่ลงตัว `ToneAnalyzer` จะคืน `ToneAnalysis.empty` ทิ้งการวิเคราะห์ทั้งรอบ

### 2.5 Apple Vision OCR และ AVSpeechSynthesizer TTS

- **Vision** (`lib/services/vision_service.dart`) — อ่านป้าย/เมนูมาเป็นวัตถุดิบสร้างบทเรียน
  ข้อเสียคือบางภาษา (เช่นฮีบรู) เครื่องอาจอ่านไม่ออกแล้ว fallback เป็นภาษาอื่นจนได้ตัวอักษรมั่ว
  จึงต้องมี `fellBackToOtherLanguage` เตือนผู้ใช้
- **TTS** — สร้างเสียงต้นแบบสำหรับเทียบทำนอง ข้อเสียคือเป็นเสียงสังเคราะห์ไม่ใช่เจ้าของภาษาจริง
  ทำนองที่เทียบจึงเป็น "ทำนองที่ระบบสังเคราะห์" และคุณภาพต่างกันมากตามรุ่น voice ที่เครื่องมี
  (`VoiceController` จึงเลือกเสียงคุณภาพดีที่สุดให้อัตโนมัติ)

---

## 3. Flow การคิดคะแนน

ทั้งหมดรวมอยู่ที่ `lib/services/take_recorder.dart` จุดเดียว เพราะหน้าฝึกกับหน้าสนทนา
ต้องได้คะแนนเท่ากันเป๊ะในเงื่อนไขเดียวกัน — ถ้าเขียนแยกกัน ความต่างจะโผล่มาเป็นคะแนนที่ไม่ตรงกัน

```
[กดอัด]
   │
   ├─ ขอสิทธิ์ไมค์ + speech recognition พร้อมกันในครั้งแรก
   ├─ หยุด TTS และเสียง playback ก่อนเปิดไมค์ (กันอัดเสียงตัวเองซ้อนเข้าไป)
   └─ เลือกทาง: native bridge (มีถอดสด) ── ถ้าเริ่มไม่ได้ ──> RecorderService สำรอง
   │
[ระหว่างพูด]
   live transcript ──> LiveMatch.matchedUnits() ──> ไฮไลต์คำ (ไม่มีผลต่อคะแนน)
   │
[กดหยุด] ──> ได้ไฟล์ WAV 16kHz mono + duration
   │
   ├───────────────── ขนานกัน (Future.wait) ─────────────────┐
   │                                                          │
 SttService.transcribe()                    SpeechAnalysisService.analyze()  [isolate]
   │ Whisper → text + segment ts               │ WavDecoder → YIN PitchTracker
   │                                           ├─ ProsodyAnalyzer → pauses, continuity
   │                                           ├─ PitchOutline    → เส้นทำนอง 48 จุด
   │                                           └─ ToneAnalyzer    → วรรณยุกต์รายพยางค์
   └──────────────────────────┬───────────────────────────────┘
                              │
            [text ว่าง → TakeFailed "ไม่ได้ยินเสียงพูด..."]
                              │
      speechDuration = prosody.speechDuration ?? stt.speechDuration
      └─ เลือก prosody ก่อนเสมอ เพราะพลังงานเสียงแม่นกว่า timestamp ของ Whisper
                              │
                   ScoringEngine.score()
                     1. normalize  (ตัดนิกกุด/วรรคตอน, lowercase, ยุบช่องว่าง)
                     2. tokenize   (คำ หรือตัวอักษรจีน)
                     3. Levenshtein DP + backtrace → List<WordDiff>
                     4. accuracy / completeness / fluency → overall
                              │
                   บันทึกลงประวัติ (Isar) — ห่อ try/catch แยกต่างหาก
                   └─ บันทึกพังต้องไม่ทำให้คะแนนที่คำนวณเสร็จแล้วหายไป
                      (พร้อม weakWords, wrongToneCharacters, toneScore, continuity)
                              │
                        แสดงผลบนจอ
                         ├─ วงแหวน overall
                         ├─ แถบ accuracy / completeness / fluency
                         ├─ diff รายคำ (สีตาม WordOp)
                         ├─ ToneChart          (เฉพาะภาษาที่มีวรรณยุกต์)
                         └─ MelodyMatch        (เมื่อกดเทียบกับเสียงต้นแบบ)
                              │
              CoachService.feedback()
                ├─ Apple FM ใช้ได้  ──> คำแนะนำภาษาไทยเฉพาะบุคคล
                └─ ใช้ไม่ได้/ไม่ตอบ ──> _ruleBasedFeedback สร้างจาก diff ตรงๆ
```

### จุดที่ห้ามแก้โดยไม่ระวัง

**`TakeRecorder._score()` ห้ามโยน exception เด็ดขาด** — หน้าจอที่เรียกตั้งสถานะเป็น
"กำลังประมวลผล" ก่อนเรียก แล้วรอผลมาเปลี่ยนสถานะกลับ ถ้า exception ทะลุออกไป `setState`
จะไม่ถูกเรียกเลย จอจะค้างหมุนตลอดกาลโดยไม่มีอะไรบอกผู้ใช้ (เคยเกิดขึ้นจริงมาแล้ว)

**การวิเคราะห์เสียงเป็นของแถม** — ถ้าอ่านไฟล์ไม่ได้ `SpeechAnalysisService` คืน
`SpeechAnalysis.empty` ไม่ล้มการให้คะแนนหลัก ผู้เรียนยังได้คะแนน แค่ไม่มีกราฟวรรณยุกต์

**DSP ต้องอยู่ใน isolate** — YIN หนักพอทำ UI กระตุก จึงส่งผ่าน `compute()` ซึ่งแปลว่า
ส่งข้ามไปได้แค่ข้อมูลล้วน (bytes + characters + pinyin) ห้ามส่ง object ที่ผูกกับ UI

---

## 4. ข้อสังเกตเชิงออกแบบ

สามข้อนี้เป็นพฤติกรรมที่ตั้งใจหรือยอมรับไว้ ณ ตอนนี้ ไม่ใช่บั๊ก — แต่ควรรู้ก่อนปรับสูตร

### 4.1 Tone / melody / continuity ไม่ถูกนับเข้า `overall`

คะแนนรวมยังเป็น text-based ล้วน 3 ตัว ผลคือผู้เรียนภาษาจีนที่ออกเสียงผิดวรรณยุกต์ทุกพยางค์
แต่ Whisper ถอดตัวอักษรถูก จะยังได้ `overall` เกือบเต็ม ทั้งที่ ToneChart ข้างล่างแดงหมด

ถ้าอยากให้คะแนนรวมสะท้อนความจริงกว่านี้ ควรถ่วง `toneScore` เข้าไปเมื่อ `analysis.hasTone`
— ค่าถูกคำนวณและเก็บลงประวัติอยู่แล้ว พร้อมใช้ทันที

### 4.2 Fluency หารด้วยจำนวนคำที่ระบบได้ยิน ไม่ใช่คำเป้าหมาย

`ScoringEngine.score()` ใช้ `spokenWords.length` เป็นตัวตั้ง ถ้าโมเดลถอดตกไปครึ่งประโยค
wpm จะร่วงตาม แล้วผู้เรียนโดนหักคะแนนความคล่องจากความผิดพลาดของโมเดล — ซึ่งซ้ำซ้อนกับที่
โดนหัก accuracy และ completeness ไปแล้วจากเหตุเดียวกัน

### 4.3 `extra` ไม่ถูกหักคะแนน

พูดเกินมา 10 คำได้คะแนนเท่ากับพูดพอดี ตั้งใจให้ผ่อนปรนกับผู้เริ่มต้น
แต่ก็แปลว่าการพูดวนซ้ำจนกว่าจะถูกไม่มีต้นทุนใดๆ

---

## ไฟล์ที่เกี่ยวข้อง

| ไฟล์ | หน้าที่ |
|---|---|
| `lib/scoring/scoring_engine.dart` | สูตรคะแนนหลัก, normalize, tokenize, alignment |
| `lib/scoring/tone_analyzer.dart` | วรรณยุกต์จีนจากเส้นเสียงจริง + สเกล Chao |
| `lib/scoring/prosody_analyzer.dart` | จังหวะ การหยุดคิด ความต่อเนื่อง |
| `lib/scoring/live_match.dart` | จับคู่คำแบบสดสำหรับไฮไลต์ (ไม่ให้คะแนน) |
| `lib/audio/pitch_tracker.dart` | YIN pitch tracking |
| `lib/audio/pitch_outline.dart` | ย่อเส้นเสียง 48 จุด + `MelodyMatch` |
| `lib/services/take_recorder.dart` | ลำดับ อัด → ถอด/วิเคราะห์ → ให้คะแนน → บันทึก |
| `lib/services/speech_analysis_service.dart` | ห่อ DSP ทั้งหมดไว้ใน isolate |
| `lib/services/stt_service.dart` | เลือก/ดาวน์โหลด/ลบโมเดล Whisper + ถอดเสียง |
| `lib/services/coach_service.dart` | คำแนะนำและแบบฝึกจาก AI พร้อมทางถอย rule-based |
| `lib/models/attempt.dart` | บันทึกการฝึก + `WeaknessReport` |
| `lib/screens/result_panel.dart` | แสดงคะแนน แถบ diff และเกณฑ์สี |
